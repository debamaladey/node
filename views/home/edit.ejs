<!DOCTYPE html>
<html>
  <head>
    <% include ../templates/head.ejs%>
  </head>
  <body>
    <form id="form" method = "POST" action = "/home-update/<%= lists.id %>" enctype="multipart/form-data">
        <label for="name">Name</label>
        <input type="text" id="name" name="name" value="<%= lists.name %>"> </br></br>
        <label for="age">Address</label>
        <input type="text" id="address" name="address" value="<%= lists.address %>"> </br></br>
        <label for="porfilepic">Image</label>
        <input type="file" name="porfilepic"><br></br>
        <% if (lists.image != '' && lists.image != 'undefined') { %> 
        <img src="../uploads/home/<%= lists.image %>" height="80" width="100" /> </br></br>
        <% } %>
        <input type="submit" value="update">
    </form>
    <% include ../templates/footer.ejs%>
  </body>
</html>